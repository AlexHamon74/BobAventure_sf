{# {% extends 'base.html.twig' %}

{% block title %}Votre profil{% endblock %}

{% block body %}
{% include 'nav2.html.twig' %}

<div class="container my-5">
    {{form_start(form)}}
    {{ form_errors(form) }}

        {{ form_row(form.secret_question) }}
        {{ form_row(form.secret_answer) }}

    {{form_row(form.submit)}}
    {{form_end(form)}} #}